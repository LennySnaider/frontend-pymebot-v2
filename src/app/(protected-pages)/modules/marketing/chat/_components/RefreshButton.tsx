'use client'

/**
 * Componente vacío que reemplaza al botón de actualización eliminado
 * Se mantiene para evitar errores en componentes que puedan estar importándolo
 */

export default function RefreshButton() {
    // Este componente ha sido desactivado intencionalmente
    return null;
}